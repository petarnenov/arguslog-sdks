// Vitest setup for Angular SDK unit tests. We don't bootstrap Angular's
// TestBed here — the units under test are exercised directly without a
// rendering platform — so no zone.js import is needed.
export {};
