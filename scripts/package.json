{
  "name": "@arguslog/scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-level helper scripts (release verification, etc.) — not published",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "echo 'no lint config — plain mjs scripts'",
    "typecheck": "echo 'no typecheck — plain mjs scripts'"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
